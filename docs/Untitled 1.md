For the work we just added a weird thing occurs where it looks like the other nord move away from the selected/dragged nord I'm working with. 


How do I get back to the projects window from the canvas.
What affordances do we need still not in the mock/ui?
What affordances in the UI don't have code? WHat does the Alamr/Alert look like whaqt's it for? What is the wave icon with 3? 

The grid background is too feint in the light version.

We can create, delete, drag all nords?

We can create, delete, reattach both line ends to other nords?

We can click and see the rollouts to edit for both nords and connections (lines)?


Labels should never get smaller than they are at 125% zoom
