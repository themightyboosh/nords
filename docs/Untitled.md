

Don't need a recents listing in the projects folder. The padding for the "All Projects" and cards themselves seems non existant ot their left sides.


Don't see Create New project.

We need to use the same Lucide icons and picker for projects as everywhere else.

Drop search projects for now.

I think having the lined-dotted canvas should function the same as it does now - but exist at start and fill the viewport (do no alter how it z axis behaves - we've had iussues desynic before)

THen create a MD that reall¥ outlines how our matrix view works. How we derive our columns, swimlines, and sort order - what we select, what we map back to, and how those values are set when a card is moved from column to column re-sorted are dragged to a new swimlane.  Views should be savable and remembered. Also, study how people typically display graph DBs in spreadsheets and DBs for inpiration - this is our kanban layer but we can have fun with it. Keep it usable and not too complex. We can use all sorts of things - drop-down properties if they exist etc. We have to be mindful of different "Nord types" and types overall so we need some thinking around what to show and build out the model and what gets viewed in a matrix.