Can the project name be larger, 
can we have a project icon selector (same for Nords)

Build the comments drawer.
Need to see the project drop-down, project browser, add create delete, clone.


THe spectrum config for the project is just for the scale of the Nord.

THe user should be able to determine the step count and what each step means (step values round up)


For Lines we need to use the same step system but it should support both X, and X + Y steps. 

This should also include directionality.

We need think about this as a reusable components. 

Same with all type properties (component thinking).l

THink deeply about the concept model here.

Additionally think through the Matrix view. If we add new Nords how it inherets the connection of the views and moving reassigns.

Once integrated update and validate aqainst all local MDs.

THen do a full review of the mockup. I want to ensure it is clean and modular suitable as a reference as we may use other toolkits.
