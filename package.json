{
  "name": "nords-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:local": "bash scripts/dev-local.sh",
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "db:migrate": "tsx db/migrate.ts",
    "db:rollback": "tsx db/migrate.ts rollback"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "node-pg-migrate": "^7.9.1",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
